---
title: "backtest-kit Redis+MongoDB Docker"
sidebarTitle: "Home"
description: "Production-grade persistence for backtest-kit using MongoDB as source of truth and Redis as an O(1) lookup cache, deployed with docker-compose."
---

backtest-kit Redis+MongoDB Docker replaces the default file-based `./dump/` persistence layer of [backtest-kit](https://github.com/tripolskypetr/backtest-kit) with **MongoDB** for durable storage and **Redis** for sub-millisecond context-key lookups. Fifteen custom `IPersist*` adapters implement the full backtest-kit persistence contract — strategy code, runners, and the CLI entrypoint stay completely unchanged.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Spin up MongoDB, Redis, and run your first backtest in under five minutes.
  </Card>
  <Card title="Architecture" icon="sitemap" href="/architecture/overview">
    Understand the two-layer persistence model, atomicity guarantees, and the Redis cache strategy.
  </Card>
  <Card title="Deployment" icon="docker" href="/deployment/docker-compose">
    One-command docker-compose deploys for local development and production.
  </Card>
  <Card title="Persist Adapters" icon="database" href="/reference/adapters-overview">
    Reference for all 15 MongoDB+Redis persist adapters and their collection schemas.
  </Card>
</CardGroup>

## Why MongoDB + Redis?

The default backtest-kit persistence writes plain files under `./dump/`. This works well for single-machine runs but breaks down in production: no concurrent access safety, no horizontal scale, and O(n) reads for large datasets.

This integration solves each issue:

<CardGroup cols={3}>
  <Card title="Atomic Upserts" icon="shield-check">
    Every write uses `findOneAndUpdate` with `upsert: true` on a compound unique index — eliminating E11000 duplicate key races entirely.
  </Card>
  <Card title="O(1) Reads" icon="bolt">
    Redis stores `context-key → MongoDB _id` mappings. Steady-state reads cost one Redis `GET` plus one MongoDB `findById` — both O(1).
  </Card>
  <Card title="Look-Ahead Safe" icon="clock">
    Signal-affecting adapters store a `when: Date` column so backtest-kit's look-ahead bias filter can verify temporal correctness.
  </Card>
</CardGroup>

## Running Modes

The project supports three execution modes switchable via CLI flags or container environment variables:

| Mode | CLI flag | `MODE` env | Description |
|---|---|---|---|
| Backtest | `--backtest` | `backtest` | Replays historical OHLCV data through your strategy |
| Paper | `--paper` | `paper` | Simulates live trading with real market data, no orders |
| Live | `--live` | `live` | Connects to a real exchange and submits orders |

<Steps>
  <Step title="Start infrastructure">
    Launch MongoDB and Redis containers with docker-compose.
  </Step>
  <Step title="Configure environment">
    Copy `.env.example` to `.env` and set your connection strings.
  </Step>
  <Step title="Build and run">
    Run `npm run start -- --entry --backtest --ui ./build/index.cjs` or use the full docker-compose stack.
  </Step>
  <Step title="View results">
    Open the backtest-kit web UI at `http://localhost:60050` to inspect signals, logs, and performance metrics.
  </Step>
</Steps>
